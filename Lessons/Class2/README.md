## Notas
Para que un script se pueda ejecutar, al crearlo debemos darle permiso para ejecutarse.
El comando para ello es `sudo chmod +x <script>`.

Para que un script sea reconocido como script de bash, debe comenzar con la línea `!#/bin/bas`.
De esta manera no hace falta agregar la extensión `.sh`.